services:
  - type: web
    name: selapan-pkl
    env: python
    buildCommand: pip install -r requirements.txt && python create_db.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
